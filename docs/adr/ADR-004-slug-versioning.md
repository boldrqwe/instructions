# ADR-004: Slug + версионирование и ревизии

- **Slug** уникален среди опубликованных версий.
- Публикация: фиксирует snapshot в `Revision`, статус `PUBLISHED`.
- Доступ по `slug` всегда отдает **актуальную опубликованную** версию.
